#!/usr/bin/garden-exec
#{
# garden env-keep-only
# . $(dirname $0)/../share/env.sh
# exec python $0 "$@"
#}

import msys.dump
rc = msys.dump.main()
print "dms-diff got rc", rc
exit(rc)
exit(msys.dump.main())

